#include "defs.h"

// FUNCTION: SHANDALAR 0x0041905e
void FUN_0041905e(int hint_index)
{
  (void)hint_index;
  // TODO(decomp): Displays a hint / info pop-up (used by the city buy-cards UI when a hint entry is selected).
}

